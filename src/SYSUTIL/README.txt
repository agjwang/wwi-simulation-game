SYSUTIL: System Utilities

- Used for things like defining certain macros such as build state (e.g. debug build), global typedefs, and anything needed that is relevant to helping to build, run, or debug the code
